extends ./layout

block content
  .page-header
    h1 Welcome, #{user.username}!
    if user.defaultCredentials
      .alert.alert-dismissible.alert-warning
        button.close(type='button', data-dismiss='alert') ×
        h4 Warning!
        p.
          It is strongly recommended to change the default credentials to avoid security issues. You may do this on your #[a.alert-link(href=rootPath+'/profile') profile page].
    p.lead Here is the list of available services: